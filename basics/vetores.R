spring_months <- c("March", "April", "May", "June")
typeof(spring_months) # Verificar o tipo dos dados
length(spring_months) # Verificar o tamanho o vector
print(spring_months[1]) # start in 1 not 0

#    index   1  2  3  4  5  6
numbers <- c(1, 2, 3, 4, 5, 6) 