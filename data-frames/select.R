# Seleciona as colunas desejadas

# Nesse exemplo a coluna group será retornada de artists
# artist_groups <- artists %>%
#   select(group)

# Multiplas colunas
# group_info <- artists %>%
  # select(group, spotify_monthly_listeners, year_founded)

# Excluindo colunas
# customers %>%
  # select(-name,-phone)